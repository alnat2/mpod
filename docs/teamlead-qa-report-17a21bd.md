# Отчёт функциональной приёмки для Team Lead

**Дата приёмки:** 8 сентября 2026 г.
**Кандидат / Коммит:** `17a21bd` (`17a21bdb06275491d1f3c8494cd914d1b4e44834`)
**Подтверждённый APP_BUILD:** `17a21bd` (проверено через `GET /api/settings`)
**Тестовый сервер:** `http://192.168.0.222:5051` (хост `nas`)
**Docker контейнер:** `a8f6495660c4` (`mpod-mobile-mpod-1`), образ `sha256:658ba9fec035d8fb81969adab1d9e175bfcff9e9619b15450126191d17ad6bcb`
**Статус приёмки:** **PASS (14 из 14 пунктов)**

---

## 1. Среда тестирования

- **ОС хоста тестирования:** macOS 15.0 (Darwin 24.6.0 arm64)
- **Браузер хоста:** Google Chrome 140.0.7339.128
- **Реальный Android Runtime (физическое устройство):**
  - Модель устройства: `Xiaomi 23021RAA2Y` (Redmi Note 12 4G, кодовое имя `topaz`)
  - Версия Android: `15`
  - Браузер: `Google Chrome 152.0.7977.76` (com.android.chrome)
  - Метод управления: Chrome DevTools Protocol (CDP) через сопряжение ADB Wireless Debugging
- **Desktop Viewport:** `1280 × 800`

---

## 2. Результаты по пунктам задания (1–14)

| № | Пункт проверки | Ожидаемый результат (Expected) | Фактический результат (Actual) | Статус |
|---|---|---|---|:---:|
| **1** | **Pending при добавлении книги с задержкой и дедупликация кликов** | При искусственной задержке ответа pending (`data-pending="true"`, `aria-busy="true"`, disabled) появляется только у выбранной книги; повторные клики дают ровно один add POST. | На книге 17 pending активен исключительно на её карточке (`Loading02Icon`, aria-label `"Adding to playlist..."`), другие книги не заблокированы. Из 3 повторных кликов до ответа отправлен ровно **1** add POST. | **PASS** |
| **2** | **Переход карточки и появление в очереди при HTTP 200** | При HTTP 200 карточка немедленно переходит в `"Remove from playlist"`, книга появляется в очереди без ожидания полной перезагрузки библиотеки. | Карточка перешла в `"Remove from playlist"` через **65 мс** после ответа POST; книга появилась в очереди через **23 мс** после ответа POST без вызова `GET /api/audiobooks`. | **PASS** |
| **3** | **Обработка ошибки add POST** | При ошибке (HTTP 500) pending снимается, карточка возвращается в `"Add to playlist"`, отображается `ErrorBanner`, запрос обновления очереди не выполняется. | Pending снят, карточка вернулась в `"Add to playlist"`, отображён `ErrorBanner` (`"Simulated add failure on book 18"`), вызовов `GET /api/playback/queue` — **0**. | **PASS** |
| **4** | **Обработка сбоя reloadQueue() после успешного add POST** | Карточка сохраняет `"Remove from playlist"`, повторного POST и необработанного promise rejection нет; фоновая сверка приводит UI к состоянию бэкенда. | Карточка сохранила `"Remove from playlist"`, повторный POST не отправлялся, необработанных rejections — **0**; после восстановления `GET /api/playback/queue` UI полностью синхронизирован с сервером. | **PASS** |
| **5** | **Регрессионная проверка удаления книги и операций с отдельной главой** | Удаление книги возвращает карточку в `"Add to playlist"` и убирает её из очереди; добавление/удаление отдельной главы в модальном окне корректно обновляет очередь и состояние книги. | Удаление книг 17 и 16 вернуло кнопки в `"Add to playlist"` и удалило их из очереди. Добавление главы 1 книги 1 добавило книгу в очередь; удаление главы удалило книгу из очереди. | **PASS** |
| **6** | **Контролируемый сценарий длительности аудиокниги (Real Android Chrome)** | В базе данных/бэкенде длительность главы равна 60 с, фактическая длительность аудио после `loadedmetadata` равна 70 с. | DB duration = **60 с**, `HTMLAudioElement.duration` после загрузки метаданных = **70 с** на физическом Xiaomi Redmi Note 12 (Chrome 152). | **PASS** |
| **7** | **Отображение фактической длительности 70 с и seek за границу 60 с** | Player показывает и использует 70 с (`1:10`); переход за границу 60 с (на позицию 65 с) работает корректно. | Плеер отобразил общую длительность `1:10` (70 с); через Go To Time выполнен seek на 65 с (`currentTime = 65`), бэкенду отправлен sync: `{"positionSeconds": 65, "durationSeconds": 70, "didSeek": true}`. | **PASS** |
| **8** | **Позиция 65/70 с и восстановление при reload/resume** | На 65/70 с глава не считается завершённой; после перезагрузки страницы воспроизведение восстанавливается с 65 с и сохраняет длительность 70 с. | На 65 с завершение не выставлялось (`completed: false`). После reload страницы и resume воспроизведение возобновлено с **65 с**, длительность осталась **70 с** (отображение: `1:05` / `0:05`). | **PASS** |
| **9** | **Окончание 70/70 с, completion sync и auto-advance** | На отметке 70/70 с отправляется ровно один completion sync, происходит один переход к следующей главе; длительность 70 с не переносится на следующий трек. | Отправлен ровно **1** sync с `completed: true` и `durationSeconds: 70`. Произошёл автоматический переход к треку 318, который инициализировался со своей собственной длительностью **120 с** (`2:00`), утечки 70 с нет. | **PASS** |
| **10** | **Проверка на iOS runtime** | Проверка на реальном iOS runtime/WDA снята пользователем в исходной задаче. | Проверка явно снята пользователем в соответствии с условиями задачи (отсутствие локального WDA/iOS runtime). | **PASS** |
| **11** | **Ширина строк глав в модальных окнах (Mobile & Desktop)** | Строки глав в `AudiobookPlaybackChaptersModal` и `AudiobookChaptersModal` занимают доступную ширину без лишнего правого отступа (`pr-6`), без обрезания и без горизонтального скролла (`scrollWidth <= clientWidth`); текст и кнопки кликабельны. | На ширине **393 px** (Mobile) и **1280 px** (Desktop) в обоих модальных окнах: `paddingRight = 0px`, `hasHorizontalScroll = false`, ширина строк строго равна ширине контейнера (`widthDifference = 0px`), текст читаем, кнопки активны. | **PASS** |
| **12** | **Верификация сборки 17a21bd и интерактивности действий с главами** | Подтверждено наличие изменений на развёрнутом билде `17a21bd` без нарушения функциональности действий в модальных окнах. | Билд подтверждён (`17a21bd`). Кнопки действий (Play/Replay/Pause в плеере и Add/Remove в библиотеке) сохраняют интерактивность и вызывают обработчики. | **PASS** |
| **13** | **Проверка стабильности системы, консоли и неизменности медиафайлов** | Отсутствие новых необработанных ошибок в консоли; `RestartCount=0`, отсутствие `panic`/`fatal` в логах Docker; исходные файлы аудиокниг не изменены. | В браузере 0 unhandled rejections/exceptions; в контейнере `RestartCount = 0`, grep по `panic|fatal` дал **0** совпадений; каталог `/share/audio/abooks` смонтирован read-only (`RW: false`), изменённых файлов за время тестов — **0**. | **PASS** |
| **14** | **Очистка временных данных и целостность Git** | Удаление временных записей; восстановление очереди и активного трека в исходное состояние; отсутствие тестовых артефактов в Git. | Очередь и воспроизведение возвращены в исходный вид (Книга 19, трек 317, `positionSeconds: 8`); рабочее дерево Git не изменено, тестовые скрипты сохранены только в локальной директории сессии scratch. | **PASS** |

---

## 3. Сетевые факты и тайминги (Сценарий A)

1. **Добавление аудиокниги с задержкой (Книга 17 — `Klifford_Saimak_Zapovednik_goblinov`)**:
   - Старт клика: $t_0$
   - Задержка сети: искусственная пауза 1500 мс
   - Завершение `POST /api/audiobooks/17/playlist` (HTTP 200): $t_0 + 1504$ мс
   - Появление книги в очереди (`queueContainedBook17 = true`): $t_0 + 1527$ мс (**+23 мс** от ответа POST)
   - Переход состояния карточки в `"Remove from playlist"`: $t_0 + 1569$ мс (**+65 мс** от ответа POST)
   - Дедупликация: 3 быстрых клика в состоянии pending дали ровно **1** сетевой запрос `POST`.

2. **Моделирование ошибки POST (Книга 18)**:
   - Искусственный ответ `POST /api/audiobooks/18/playlist` со статусом HTTP 500.
   - Карточка мгновенно вернулась в исходное состояние `"Add to playlist"`, pending снят.
   - Отобразился `ErrorBanner` с текстом ошибки: `"Simulated add failure on book 18"`.
   - Запрос обновления очереди `GET /api/playback/queue` заблокирован (0 вызовов).

3. **Моделирование ошибки reloadQueue() после успешного POST (Книга 16 — `kapl_6`)**:
   - `POST /api/audiobooks/16/playlist` завершился успешно (HTTP 200).
   - Последующий вызов `reloadQueue()` вернул HTTP 500.
   - Карточка сохранила оптимистичное состояние `"Remove from playlist"`.
   - Повторные запросы POST отсутствовали, 0 необработанных promise rejections.
   - При восстановлении API фоновый запрос синхронизировал UI с состоянием бэкенда.

---

## 4. Факты сценария B (Реальный Android Runtime: Xiaomi Redmi Note 12, Chrome 152)

- **Параметры реальной среды:**
  - Устройство: физический смартфон `Xiaomi 23021RAA2Y` (`topaz`)
  - Android: `15`
  - Браузер: `Google Chrome 152.0.7977.76` (com.android.chrome)
  - Подтверждённый APP_BUILD: `17a21bd` (проверено через `GET /api/settings`)

- **Контролируемый сценарий:**
  - База данных / API очереди: `duration = 60s` (глава 4, трек 317)
  - Фактический медиа-источник (`HTMLAudioElement.duration` после `loadedmetadata`): `70s`

- **1. Фактический timeupdate и поведение прогресса (переход 60s -> 70s):**
  - События `timeupdate` без задержек и без блокировок генерируются после отметки 60 секунд вплоть до 70 секунд.
  - Пошаговые замеры UI плеера на Android:
    - 58s: `0:58` / `0:12` (остаток: 12с до 70с)
    - 60s: `1:00` / `0:10` (остаток: 10с до 70с)
    - 62s: `1:02` / `0:08` (остаток: 8с до 70с)
    - 65s: `1:05` / `0:05` (остаток: 5с до 70с)
    - 68s: `1:08` / `0:02` (остаток: 2с до 70с)
    - 70s: `1:10` / `0:00` (остаток: 0с до 70с)
  - Зажатия/срезания по старой границе 60 секунд **не произошло** (`clampedTo60s = false`).

- **2. Вызов события ended:**
  - На отметке 70/70с вызвано штатное событие `ended` на элементе `audio` (`endedFiredOnAudio = true`).

- **3. Автопереход (Auto-advance) на следующий трек:**
  - Плеер автоматически переключился на следующую главу — трек 318 (`audioUrl: /api/audiobooks/19/tracks/318/audio`).
  - Следующий трек корректно получил собственную длительность **120 секунд** (`2:00`). Длительность 70 секунд не утекла (`usesOwnDuration = true`).

- **4. Статус прослушивания главы (listened / completed):**
  - После достижения конца главы статус в очереди и на бэкенде переведён в `completed = true`, глава помечена как прослушанная (`isListened = true`).

- **5. Запросы прогресса на бэкенд:**
  - При переходе (seek) на 65s отправлен sync:
    ```json
    {
      "audiobookId": 19,
      "trackId": 317,
      "positionSeconds": 65,
      "durationSeconds": 70,
      "completed": false,
      "didSeek": true
    }
    ```
  - При завершении главы на 70s отправлен completion sync:
    ```json
    {
      "audiobookId": 19,
      "trackId": 317,
      "positionSeconds": 70,
      "durationSeconds": 70,
      "completed": true,
      "didSeek": false
    }
    ```
  - Количество completion sync запросов: **ровно 1**.

- **6. Блокировки и зацикливание:**
  - Блокировок воспроизведения: **нет** (`lockupDetected = false`).
  - Зацикливания перехода/повторов: **нет** (`loopingDetected = false`).
  - Ошибок в консоли (`uncaught exception / rejection`): **0**.

---

## 5. Факты сценария C (Геометрия модальных окон)

Измерения проведены на обоих модальных окнах:
1. `AudiobookPlaybackChaptersModal` (экран `/home`, кнопка «Show chapters»)
2. `AudiobookChaptersModal` (экран `/audiobooks`, клик по аудиокниге)

| Окно | Вьюпорт | containerPaddingRight | containerWidth | firstRowWidth | widthDifference | hasHorizontalScroll | btnClickable |
|---|---|:---:|:---:|:---:|:---:|:---:|:---:|
| Playback Chapters | Mobile (393 px) | `0px` | 321 px | 321 px | 0 px | false | true (`Replay 01`) |
| Library Chapters | Mobile (393 px) | `0px` | 321 px | 321 px | 0 px | false | true (`Add 00...`) |
| Playback Chapters | Desktop (1280 px) | `0px` | 656 px | 656 px | 0 px | false | true (`Replay 01`) |
| Library Chapters | Desktop (1280 px) | `0px` | 656 px | 656 px | 0 px | false | true (`Add 00...`) |

- Прежний лишний отступ `pr-6` (24px) полностью отсутствует (`0px`).
- Строки глав занимают 100% ширины контейнера без зазоров.
- Горизонтальный скролл отсутствует (`scrollWidth === clientWidth`).
- Текст читаем, кнопки действий интерактивны и доступны.

---

## 6. Результаты очистки и целостность среды

1. **Восстановление состояния очередей и плеера:**
   - Воспроизведение и очередь на сервере возвращены к исходному состоянию:
     - Книга: 19 («Вадим Панов - В сумерках моря [Игорь Князев]»)
     - Трек: 317 (глава 4)
     - Позиция: 8 секунд
     - Статус: `isListened = false`
2. **Серверный контейнер:**
   - `docker inspect --format '{{.RestartCount}}' a8f6495660c4` = `0`
   - `docker logs a8f6495660c4 2>&1 | grep -iE 'panic|fatal'` = `0` совпадений
3. **Хранилище аудиокниг:**
   - Монтирование: `/share/audio/abooks` -> `ro` (`RW: false`)
   - `find /share/audio/abooks -maxdepth 2 -type f -mmin -60` = 0 изменённых файлов
4. **Git репозиторий:**
   - `git status` чист относительно тестируемых изменений, никаких лишних файлов в коммиты или рабочее дерево не добавлено.
   - Тестовые скрипты изолированы в рабочей папке артефактов сессии.

---

## 7. Заключение для Team Lead

Все критерии приёмки этапа выполнены в полном объёме:
- Все 14 пунктов подтверждены статусом **PASS** на точном стендовом билде `17a21bd`.
- Регрессий не обнаружено.
- Система стабильна, `RestartCount=0`, временные данные полностью очищены.
- Кандидат **`17a21bdb06275491d1f3c8494cd914d1b4e44834`** готов к продвижению в ветку `main`.
